# Release v0.7.3 — Prompt History Restyling, Full-Bleed Response Blocks & Release Links

> Restyled Development Prompts with a complete visual post-style header, full-bleed edge-to-edge subtle grey response blocks, provenance badges, and milestone release tag links.

---

### Key Changes

1. **Development Prompts Header**:
   - Single-line post-style header with back arrow (`?`), dual provenance badge (`[Mine] [AI]`), page title (`1.15rem`), and right-aligned date.
   - Cleaned redundant intro paragraph metadata text.

2. **Prompt & Response Visual Hierarchy**:
   - Mine icon + `Jim:` for prompts.
   - AI icon + `Response:` on full-bleed edge-to-edge subtle grey blocks (`100vw` with `overflow-x: clip`).
   - Content constrained to standard reading container width.

3. **Release Milestone Links**:
   - Milestone titles scaled to `1.08rem` and linked directly to corresponding GitHub release tags with right-aligned dates.
