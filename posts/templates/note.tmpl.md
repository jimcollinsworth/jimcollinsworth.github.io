---
title: ${title}
slug: ${slug}
date: ${date}
type: note
note_type: ${note_type}  # TIL, opinion, goal
tags: ${tags}
category: ${category}
status: draft  # draft, published
---

${content}

${references}